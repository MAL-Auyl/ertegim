# Распознавание детской речи (этап 1b) — план реализации

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** Поднять точность распознавания детских ответов без смены модели: pre-roll и детские пороги VAD, подсказка Whisper по текущему вопросу, фонетическое сравнение в локальном классификаторе.

**Architecture:** Чистые функции выносятся в отдельные файлы с тестами (`app/public/preroll.js`, `spike/stt-hints-core.js`, расширение `app/public/classify-local.js`); `app.js` меняется точечно (VAD-блок, `submitAudio`); оба серверных STT-эндпоинта читают `nodeId`/`brotherName` из формы и строят промпт через общий ESM-модуль. Серверы здесь не запускаются — только `node --check` + unit-тесты.

**Tech Stack:** Vanilla JS classic scripts (browser) + ESM для `spike/`, Bun test, Groq Whisper API.

**Spec:** `docs/superpowers/specs/2026-09-17-child-stt-design.md`

## Global Constraints

- Ветка `story-and-parent-report`; работать в `app/public/`, `spike/`, `app/tests/`; в `app/server.js` и `api/transcribe.js` менять только чтение полей формы и построение промпта (+`temperature`).
- Пороги VAD: `VAD_START_RMS = 0.014`, `VAD_SILENCE_RMS = 0.009`, `VAD_SILENCE_MS = 1400`, `VAD_MIN_SPEECH_MS = 300`, `VAD_MAX_RECORD_MS = 8000`, `VAD_CHUNK_MS = 250`, `VAD_PREROLL_CHUNKS = 2`.
- Поля формы: `nodeId` (string), `brotherName` (string, lowercase kk), оба необязательные.
- `recordingStartedAt` = момент старта речи, не старта рекордера.
- Коммиты завершать строкой `Co-Authored-By: Claude Fable 5.1 <noreply@anthropic.com>`.
- Тесты: `cd app && bun test` (сейчас 40 зелёных).

---

### Task 1: `spike/stt-hints-core.js` — подсказка Whisper по узлу

**Files:**
- Create: `spike/stt-hints-core.js`
- Test: `app/tests/stt-hints.test.js`

**Interfaces:**
- Produces (ESM): `sttHintFor(nodeId, ctx = {}) → string`, `DEFAULT_HINT` (строка = текущий `GROQ_PROMPT`).

- [ ] **Step 1: Тест**

```js
const { test, expect } = require("bun:test");

test("stt hints per node", async () => {
  const { sttHintFor, DEFAULT_HINT } = await import("../../spike/stt-hints-core.js");
  expect(sttHintFor("q_tracks")).toContain("бір, екі, үш, төрт, бес");
  expect(sttHintFor("q_tracks")).toContain("один, два, три, четыре, пять");
  expect(sttHintFor("q_fork")).toContain("солға");
  expect(sttHintFor("q_fork")).toContain("направо");
  expect(sttHintFor("q_courage")).toContain("қорықпа");
  expect(sttHintFor("q_echo", { brotherName: "мысық" })).toContain("мысық");
  expect(sttHintFor("q_echo", { brotherName: "мысық" })).toContain("қасық");
  expect(sttHintFor("q_echo")).toContain("балық"); // no name → still has the family
  expect(sttHintFor("intro")).toBe(DEFAULT_HINT);
  expect(sttHintFor(undefined)).toBe(DEFAULT_HINT);
  expect(sttHintFor("q_echo", { brotherName: "<script>" })).not.toContain("<");
});
```

- [ ] **Step 2: Запустить — падает** (`cd app && bun test tests/stt-hints.test.js` → module not found)

- [ ] **Step 3: Реализовать `spike/stt-hints-core.js`**

```js
// Per-question vocabulary hint for Whisper's `prompt` field. Shared by
// app/server.js (Bun) and api/transcribe.js (Vercel Edge) — plain ESM, no
// Bun/DOM APIs, same split as blocklist-core.js. A prompt that names only
// the words a child could plausibly answer with biases the decoder far
// harder than one generic list for every question.

const DEFAULT_HINT =
  "Сәлем, түлкі, үкі, жидек, санау, ұйқас, мысық, балық, қасық, дұрыс, ойнайық, " +
  "бір, екі, үш, төрт, бес, один, два, три, четыре, пять";

const HINTS = {
  q_tracks: "із, санау, бір, екі, үш, төрт, бес, один, два, три, четыре, пять",
  q_fork: "солға, оңға, өзен, орман, сол жақ, оң жақ, налево, направо, река, лес",
  q_courage: "қорықпа, мен сенімен біргемін, батыл, бәрі жақсы, не бойся, я с тобой, ты смелый, всё хорошо",
};

function cleanName(name) {
  return String(name || "").toLowerCase().replace(/[^\p{L}]/gu, "").slice(0, 24);
}

function sttHintFor(nodeId, ctx = {}) {
  if (nodeId === "q_echo") {
    const name = cleanName(ctx.brotherName);
    return `${name ? name + ", " : ""}балық, мысық, қасық, ұйқас, жаңғырық`;
  }
  return HINTS[nodeId] || DEFAULT_HINT;
}

export { sttHintFor, DEFAULT_HINT };
```

- [ ] **Step 4: Тесты зелёные** (`cd app && bun test`)
- [ ] **Step 5: Commit** — `git add spike/stt-hints-core.js app/tests/stt-hints.test.js && git commit -m "Add per-question STT vocabulary hints (stt-hints-core.js)\n\nCo-Authored-By: Claude Fable 5.1 <noreply@anthropic.com>"`

---

### Task 2: Фонетическая нормализация и детские формы чисел в `classify-local.js`

**Files:**
- Modify: `app/public/classify-local.js`
- Test: `app/tests/classify-local.test.js` (дополнить)

**Interfaces:**
- Produces: `phonetic(word) → string` (экспорт), `NUM_FORMS` (экспорт, массив по индексу 1..5), `fuzzyIncludes` сравнивает нормализованные формы. Сигнатура `localClassify(node, transcript, ctx)` не меняется; `ctx.numKk/numRu` остаются (используются как первые элементы форм).

- [ ] **Step 1: Дописать тесты** в `app/tests/classify-local.test.js`

```js
const { phonetic, NUM_FORMS } = require("../public/classify-local.js");

describe("phonetic", () => {
  test("collapses Kazakh-specific letters to their nearest Russian sound", () => {
    expect(phonetic("қасық")).toBe("касик");
    expect(phonetic("үш")).toBe("уш");
    expect(phonetic("төрт")).toBe("торт");
    expect(phonetic("өзен")).toBe("озен");
  });
  test("drops soft/hard signs and doubled letters", () => {
    expect(phonetic("пять")).toBe("пят");
    expect(phonetic("аллея")).toBe("алея");
  });
});

describe("count with child pronunciations", () => {
  const node = { mode: "exact", skill: "count" };
  for (const [n, forms] of [[3, ["уч", "тли", "тьли", "тры"]], [4, ["четыле", "торт"]], [5, ["пат", "бес", "пяць"]], [2, ["еки", "дфа"]], [1, ["бир", "адин"]]]) {
    for (const f of forms) {
      test(`${f} → ${n}`, () => {
        expect(localClassify(node, f, { ...ctx, trackCount: n }).label).toBe("correct");
      });
    }
  }
  test("wrong number still unclear", () => {
    expect(localClassify(node, "уш", { ...ctx, trackCount: 4 }).label).toBe("unclear");
  });
  test("NUM_FORMS has 5 entries, each non-empty", () => {
    expect(NUM_FORMS.length).toBe(6);
    for (let i = 1; i <= 5; i++) expect(NUM_FORMS[i].length).toBeGreaterThan(2);
  });
});

describe("rhyme with phonetic suffix", () => {
  const node = { mode: "exact", skill: "rhyme" };
  test("қасик (mixed script) counts", () => {
    expect(localClassify(node, "қасик", ctx).label).toBe("correct");
  });
});
```

- [ ] **Step 2: Запустить — падают новые тесты**

- [ ] **Step 3: Реализовать** — в `classify-local.js` после `wordsOf`:

```js
// Children and STT both "simplify" Kazakh-specific sounds the same way
// (қ→к, ү→у, і→и …), so answers are compared as rough phonetic strings,
// not letter-for-letter. Applied to both sides of every fuzzy match.
const PHONETIC_MAP = {
  қ: "к", ғ: "г", ң: "н", һ: "х", ә: "а", ө: "о", ү: "у", ұ: "у", і: "и", ы: "и",
  э: "е", ё: "е", й: "и", ь: "", ъ: "",
};
function phonetic(word) {
  const mapped = String(word || "")
    .toLowerCase()
    .split("")
    .map((ch) => (ch in PHONETIC_MAP ? PHONETIC_MAP[ch] : ch))
    .join("");
  return mapped.replace(/(.)\1+/g, "$1");
}

// Accepted spoken forms of 1..5: canonical kk/ru plus the distortions a
// 3-7-year-old (and Whisper on a 3-7-year-old) actually produce.
const NUM_FORMS = [
  [],
  ["бір", "бир", "один", "адин", "раз", "1"],
  ["екі", "еки", "два", "дфа", "2"],
  ["үш", "уш", "уч", "три", "тры", "тли", "тьли", "3"],
  ["төрт", "торт", "четыре", "четыле", "четыри", "4"],
  ["бес", "пять", "пяць", "пат", "5"],
];
```

Заменить `fuzzyIncludes`:
```js
function fuzzyIncludes(words, target) {
  const t = phonetic(target);
  if (!t) return false;
  return words.some((w) => levenshtein(phonetic(w), t) <= tolerance(t.length));
}
```

В `localClassify`, ветка `count`:
```js
    const accepted = [ctx.numKk?.[n], ctx.numRu?.[n], String(n), ...(NUM_FORMS[n] || [])].filter(Boolean);
```
Ветка `rhyme`:
```js
    const rhymes = words.some((w) => /(ик|ык)$/.test(phonetic(w)));
```
Экспорт: добавить `phonetic, NUM_FORMS`.

- [ ] **Step 4: Все тесты зелёные** — проверить, что старые (`"тры штуки"`, `"столик"`, `"үй"` → unclear) не сломались.
- [ ] **Step 5: Commit** — `git add app/public/classify-local.js app/tests/classify-local.test.js && git commit -m "classify-local: phonetic matching + child number forms\n\nCo-Authored-By: Claude Fable 5.1 <noreply@anthropic.com>"`

---

### Task 3: Pre-roll и детские пороги VAD; `nodeId`/`brotherName` в форме

**Files:**
- Create: `app/public/preroll.js`
- Test: `app/tests/preroll.test.js`
- Modify: `app/public/app.js` (VAD-блок строки ~164–320, `onRecordingStop`, `submitAudio`), `app/public/index.html` (подключить `preroll.js` перед `app.js`)

**Interfaces:**
- Produces: `assembleClip(chunks, speechIdx, preRollChunks) → Blob[]`, `trimIdle(chunks, preRollChunks) → Blob[]` (чистые, работают с любыми массивами).

- [ ] **Step 1: Тест `app/tests/preroll.test.js`**

```js
const { test, expect } = require("bun:test");
const { assembleClip, trimIdle } = require("../public/preroll.js");

test("assembleClip keeps header + preroll + speech", () => {
  const c = ["H", "a", "b", "c", "d", "e", "f"];
  expect(assembleClip(c, 4, 2)).toEqual(["H", "c", "d", "e", "f"]);
});
test("assembleClip never duplicates the header when preroll reaches it", () => {
  expect(assembleClip(["H", "a", "b"], 1, 2)).toEqual(["H", "a", "b"]);
  expect(assembleClip(["H", "a", "b"], 0, 2)).toEqual(["H", "a", "b"]);
});
test("assembleClip with empty input", () => {
  expect(assembleClip([], 0, 2)).toEqual([]);
});
test("trimIdle keeps header and the last N chunks", () => {
  expect(trimIdle(["H", "a", "b", "c", "d"], 2)).toEqual(["H", "c", "d"]);
  expect(trimIdle(["H", "a"], 2)).toEqual(["H", "a"]);
  expect(trimIdle(["H"], 2)).toEqual(["H"]);
});
```

- [ ] **Step 2: Запустить — падает**

- [ ] **Step 3: `app/public/preroll.js`**

```js
// Pre-roll assembly for the VAD recorder. MediaRecorder's first timeslice
// chunk carries the container header (WebM EBML / fMP4 init segment), so
// any clip we build must start with chunks[0]; everything else is a plain
// media cluster and can be dropped or kept freely. Pure array logic so it
// can be unit-tested without a browser.

function assembleClip(chunks, speechIdx, preRollChunks) {
  if (!chunks.length) return [];
  const from = Math.max(1, speechIdx - preRollChunks);
  return [chunks[0], ...chunks.slice(from)];
}

// While the child is still silent the buffer would grow without bound —
// keep only the header plus the last `preRollChunks` clusters.
function trimIdle(chunks, preRollChunks) {
  if (chunks.length <= 1 + preRollChunks) return chunks;
  return [chunks[0], ...chunks.slice(chunks.length - preRollChunks)];
}

if (typeof module !== "undefined") {
  module.exports = { assembleClip, trimIdle };
}
```

- [ ] **Step 4: `app.js` — константы и состояние**

Заменить блок `const VAD_START_RMS … VAD_MAX_RECORD_MS` на:
```js
// Tuned for 3-7-year-olds (quieter than adults, pause mid-phrase): lower
// start/keep thresholds, longer end-of-turn silence. Re-check against real
// child recordings — these are informed guesses, not measurements.
const VAD_START_RMS = 0.014;
const VAD_SILENCE_RMS = 0.009;
const VAD_SILENCE_MS = 1400;
const VAD_MIN_SPEECH_MS = 300;
const VAD_MAX_RECORD_MS = 8000;
// The recorder runs the whole time a question is armed, in small slices,
// so the clip can start ~500 ms BEFORE the amplitude gate tripped — the
// first consonant of a child's answer is exactly what the gate misses.
const VAD_CHUNK_MS = 250;
const VAD_PREROLL_CHUNKS = 2;
let vadChunks = [];
let vadSpeechChunkIdx = 0;
let vadSpoke = false; // speech was detected during this arming (independent of chunk timing)
```

- [ ] **Step 5: `app.js` — рекордер запускается при arming**

Заменить `startVadRecorder`/`stopVadRecorder`/`finishVadTurn`/`armVadForQuestion`/`disarmVad`/`vadLoop` следующим образом (остальное в этих функциях не менять):

```js
function startVadRecorder() {
  const mimeType = pickMimeType();
  vadRecorder = mimeType ? new MediaRecorder(vadStream, { mimeType }) : new MediaRecorder(vadStream);
  currentMimeType = vadRecorder.mimeType || mimeType || "audio/webm";
  vadChunks = [];
  vadSpeechChunkIdx = 0;
  vadSpoke = false;
  vadRecorder.ondataavailable = (e) => {
    if (e.data && e.data.size) vadChunks.push(e.data);
    if (!vadRecording) vadChunks = trimIdle(vadChunks, VAD_PREROLL_CHUNKS);
  };
  vadRecorder.onstop = onVadRecordingStop;
  vadRecorder.start(VAD_CHUNK_MS);
}

function markVadSpeechStart() {
  vadSpeechChunkIdx = vadChunks.length;
  vadSpoke = true;
  recordingStartedAt = Date.now();
  vadRecording = true;
  recordBtn.classList.add("recording");
  statusText.textContent = "Слушаю...";
  setStage("mic", "running", "запись");
}

function stopVadRecorder() {
  if (vadRecorder && vadRecorder.state !== "inactive") vadRecorder.stop(); // keeps vadStream alive
  vadRecording = false;
  recordBtn.classList.remove("recording");
}

async function onVadRecordingStop() {
  const parts = assembleClip(vadChunks, vadSpeechChunkIdx, VAD_PREROLL_CHUNKS);
  vadChunks = [];
  if (!vadSpoke || !parts.length) return; // disarmed without speech (question changed) — nothing to send
  chunks = parts;
  await onRecordingStop();
}
```
`finishVadTurn` — без изменений (вызывает `stopVadRecorder`). В `armVadForQuestion` после успешного `ensureMicStream()` добавить запуск рекордера:
```js
  ensureMicStream()
    .then(() => { if (vadArmed && !vadRecorder) startVadRecorder(); })
    .catch((err) => { /* existing catch body unchanged */ });
```
При этом в начале `armVadForQuestion`: `vadRecorder = null;` не ставить — вместо этого в `disarmVad()`:
```js
function disarmVad() {
  vadArmed = false;
  heroStage.classList.remove("hero-listening");
  setStage("mic", "idle", "");
  vadRecording = false;
  if (vadRecorder && vadRecorder.state !== "inactive") vadRecorder.stop(); // onVadRecordingStop discards if !vadSpoke
  vadRecorder = null;
}
```
Внимание: `disarmVad` вызывается из `renderState` до `armVadForQuestion`, так что для каждого вопроса — новый рекордер. В `vadLoop` заменить ветку старта:
```js
  if (!vadRecording) {
    if (rms > VAD_START_RMS && vadRecorder && vadRecorder.state === "recording") {
      vadSpeechStartedAt = now;
      vadLastLoudAt = now;
      markVadSpeechStart();
    }
    return;
  }
```
В обработчике `recordBtn` ветка `else if (vadArmed)` → `markVadSpeechStart()` вместо `startVadRecorder()` (если `vadRecorder` ещё не стартовал — оставить fallback `startRecording()`):
```js
  } else if (vadArmed && vadRecorder && vadRecorder.state === "recording") {
    vadSpeechStartedAt = performance.now();
    vadLastLoudAt = performance.now();
    markVadSpeechStart();
  }
```
`onRecordingStop`: без изменений (использует `chunks`, `recordingStartedAt`). `stopVadRecorder` в `finishVadTurn` теперь приводит к `onVadRecordingStop` → `onRecordingStop` → `submitAudio`.

- [ ] **Step 6: `submitAudio` — поля формы**

После `form.append("audio", blob, filename);`:
```js
  form.append("nodeId", currentId || "");
  form.append("brotherName", brotherName?.kkLower || "");
```

- [ ] **Step 7: `index.html`** — добавить `<script src="preroll.js"></script>` перед `<script src="app.js">`.

- [ ] **Step 8: Проверка** — `node --check app/public/app.js`; `cd app && bun test`; `grep -n "startVadRecorder\|markVadSpeechStart\|onVadRecordingStop\|trimIdle\|assembleClip" app/public/app.js` — все вызовы определены; `grep -c "vadRecorder.start()" app/public/app.js` → 0 (везде `start(VAD_CHUNK_MS)`).
- [ ] **Step 9: Commit** — `git add app/public/preroll.js app/tests/preroll.test.js app/public/app.js app/public/index.html && git commit -m "VAD: 500ms pre-roll, child-tuned thresholds, send nodeId/brotherName with the clip\n\nCo-Authored-By: Claude Fable 5.1 <noreply@anthropic.com>"`

---

### Task 4: Серверы — промпт по узлу, `temperature: 0`

**Files:**
- Modify: `app/server.js` (`GROQ_PROMPT`, `transcribeGroq`, `transcribe`, обработчик `/api/transcribe`)
- Modify: `api/transcribe.js` (то же)

- [ ] **Step 1: `app/server.js`**
  - `import { sttHintFor } from "../spike/stt-hints-core.js";`
  - Удалить константу `GROQ_PROMPT` и её комментарий.
  - `async function transcribeGroq(audioBuf, ext, hint)` — `form.append("prompt", hint); form.append("temperature", "0");`
  - `async function transcribe(audioBuf, ext, hint)` → `transcribeGroq(audioBuf, ext, hint)`.
  - В обработчике: 
    ```js
        const nodeId = typeof form.get("nodeId") === "string" ? form.get("nodeId") : "";
        const brotherName = typeof form.get("brotherName") === "string" ? form.get("brotherName") : "";
        const hint = sttHintFor(nodeId, { brotherName });
        const { transcript, ms, engine } = await transcribe(buf, ext, hint);
    ```
- [ ] **Step 2: `api/transcribe.js`** — аналогично: `import { sttHintFor } from "../spike/stt-hints-core.js";`, удалить `GROQ_PROMPT`, `transcribeGroq(audioBuf, ext, hint)`, читать поля, `temperature "0"`.
- [ ] **Step 3: Проверка** — `node --check app/server.js api/transcribe.js` (по одному); `grep -n GROQ_PROMPT app/server.js api/transcribe.js` → пусто; `cd app && bun test`.
- [ ] **Step 4: Commit** — `git add app/server.js api/transcribe.js && git commit -m "STT: per-question Whisper prompt from stt-hints-core, temperature 0\n\nCo-Authored-By: Claude Fable 5.1 <noreply@anthropic.com>"`

---

### Task 5: Статус

**Files:**
- Modify: `docs/status-report-2026-09-17.md` — добавить раздел «Этап 1b — распознавание детской речи» (≤12 строк): что сделано (3 пункта), что НЕ проверено живьём (pre-roll-клип на Windows/Vercel: декодирование WebM без части кластеров; влияние порогов на реальных детях), как проверить (оператор слушает `#player`; сравнить транскрипты до/после на 10 записях).
- [ ] Commit — `git add docs/status-report-2026-09-17.md && git commit -m "Status: stage 1b child-STT notes\n\nCo-Authored-By: Claude Fable 5.1 <noreply@anthropic.com>"`
